[project]
name = "books-recommender-engine"
version = "0.1.0"
description = "A recommendation engine for books using machine learning."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "numpy>=2.4.0",
    "pandas>=2.3.3",
    "scikit-learn>=1.8.0",
]
